[project]
name = "REXP"
version = "0.0.1"
description = "AI EXP Framework."
requires-python = ">=3.11"

[project.scripts]
rexp = "REXP.run:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
